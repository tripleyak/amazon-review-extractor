# Optional server-side defaults. The UI also lets you paste these per-session.
# None are required for anonymous mode.

# Apify actor override (default: junglee~amazon-reviews-scraper)
APIFY_ACTOR=junglee~amazon-reviews-scraper
